<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/sherwood_green</item>
        <item name="colorPrimaryDark">@color/bush</item>
    </style>

    <style name="PaymentButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">bottom</item>
        <item name="android:background">@drawable/pay_button_selector</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>